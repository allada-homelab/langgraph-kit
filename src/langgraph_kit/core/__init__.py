"""Core building blocks for LangGraph agent systems.

Subpackages:
    commands         Slash-command dispatch and middleware
    context_management  Context pressure, compaction, continuation
    hitl             Human-in-the-loop interrupt models
    memory           Persistent memory, extraction, consolidation
    orchestration    Async tasks, message queuing, worker verification
    plugins          MCP integration and plugin registry
    prompt_assembly  Layered prompt composition and caching
    resilience       Error recovery and completion guard middleware
    skills           Progressive skill disclosure from SKILL.md
    tools            Tool capability registry and filtering
"""
